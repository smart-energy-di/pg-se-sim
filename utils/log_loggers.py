# -*- coding: utf-8 -*-
import logging

MODEL_LOG = logging.getLogger(name="model")
PLUGIN_LOG = logging.getLogger(name="plugin")
EVENT_LOG = logging.getLogger(name="event")
SIMULATION_LOG = logging.getLogger(name="simulation")
